diff --git a/engines/sci/engine/kstring.cpp b/engines/sci/engine/kstring.cpp
index fe35fd19..4b303585 100644
--- a/engines/sci/engine/kstring.cpp
+++ b/engines/sci/engine/kstring.cpp
@@ -207,6 +207,58 @@ reg_t kReadNumber(EngineState *s, int argc, reg_t *argv) {
 }
 
 
+// CHT: return the ordered sequence of printf-style conversion chars (s/d/c/x/u...)
+// in a format template, ignoring width/flag digits. Two templates with identical
+// sequences consume kFormat arguments in the same order/type.
+static Common::String sciChtFormatSpecs(const char *p) {
+	Common::String out;
+	while (*p) {
+		if (*p == '%') {
+			++p;
+			if (*p == '%') { ++p; continue; } // literal %%
+			while (*p && (Common::isDigit(*p) || *p == '-' || *p == '=')) ++p;
+			if (*p) { out += *p; ++p; }
+		} else {
+			++p;
+		}
+	}
+	return out;
+}
+
+// CHT: map a translated template's specifiers onto the original's. Succeeds when the
+// translation's specifier sequence is an in-order subsequence of the source's -- this
+// lets a translation drop source-only markers (e.g. English plural "%s" that Chinese
+// grammar omits) while still consuming the RIGHT arguments. On success returns the
+// number of translated specifiers and fills argMap[j] with the source-specifier index
+// (= source argument index) that the j-th translated specifier must consume, so kFormat
+// can compact the argument list and skip the dropped ones.
+//
+// SAFETY: kFormat reads %d/%c/%x/%u from the (remappable) `arguments[]` array but reads
+// %s directly from `argv[startarg + paramindex]`, which our remap can't reach. So a
+// dropped/shifted %s in the translation would desync. We therefore only accept a
+// NON-identity subsequence when the translation contains no %s at all (then every kept
+// specifier goes through `arguments[]` and the remap is exact). An identical specifier
+// sequence is always safe (the remap is a no-op and paramindex stays aligned). Returns
+// -1 (keep original English template) otherwise, or if there are more than maxMap specs.
+static int sciChtMapFormatSpecs(const char *src, const char *cht, int *argMap, int maxMap) {
+	Common::String s = sciChtFormatSpecs(src);
+	Common::String c = sciChtFormatSpecs(cht);
+	if ((int)c.size() > maxMap)
+		return -1;
+	uint si = 0;
+	for (uint ci = 0; ci < c.size(); ++ci) {
+		while (si < s.size() && s[si] != c[ci])
+			++si;
+		if (si >= s.size())
+			return -1;
+		argMap[ci] = (int)si;
+		++si;
+	}
+	if (s != c && c.contains('s'))
+		return -1; // dropped-spec remap with a surviving %s is unsafe (argv-direct read)
+	return (int)c.size();
+}
+
 /*  Format(targ_address, textresnr, index_inside_res, ...)
 ** or
 **  Format(targ_address, heap_text_addr, ...)
@@ -249,6 +301,20 @@ reg_t kFormat(EngineState *s, int argc, reg_t *argv) {
 
 	int index = (startarg == 3) ? argv[2].toUint16() : 0;
 	Common::String source_str = g_sci->getKernel()->lookupText(position, index);
+	// CHT: translate the format TEMPLATE (with literal %s/%d) before substitution, so
+	// player names/numbers land inside the Chinese template. The GfxText16 content-match
+	// hook cannot catch these -- it only sees the post-substitution string. The Chinese
+	// template may legitimately use fewer specifiers than the English one (dropping
+	// English-only plural markers), so we compute a subsequence arg-mapping and remap the
+	// argument list below; a template whose specifiers don't map falls back to English.
+	int chtArgMap[16];
+	int chtArgCount = -1;
+	const char *chtTemplate = g_sci->getChtTranslation(source_str.c_str());
+	if (chtTemplate) {
+		chtArgCount = sciChtMapFormatSpecs(source_str.c_str(), chtTemplate, chtArgMap, 16);
+		if (chtArgCount >= 0)
+			source_str = chtTemplate;
+	}
 	const char* source = source_str.c_str();
 
 	debugC(kDebugLevelStrings, "Formatting \"%s\"", source);
@@ -260,6 +326,18 @@ reg_t kFormat(EngineState *s, int argc, reg_t *argv) {
 	for (i = startarg; i < argc; i++)
 		arguments[i-startarg] = argv[i].toUint16(); /* Parameters are copied to prevent overwriting */
 
+	// CHT: when a translated template was swapped in above, reorder the arguments so its
+	// (possibly fewer) specifiers pull the correct source arguments, skipping any the
+	// translation dropped. chtArgMap[j] is the source argument index for the j-th kept
+	// specifier; consumption below is positional so we compact them to the front.
+	if (chtArgCount >= 0) {
+		uint16 remapped[16];
+		for (int j = 0; j < chtArgCount; j++)
+			remapped[j] = arguments[chtArgMap[j]];
+		for (int j = 0; j < chtArgCount; j++)
+			arguments[j] = remapped[j];
+	}
+
 	while ((xfer = *source++)) {
 		if (xfer == '%') {
 			if (mode == 1) {
@@ -311,6 +389,17 @@ reg_t kFormat(EngineState *s, int argc, reg_t *argv) {
 
 				Common::String tempsource = g_sci->getKernel()->lookupText(reg,
 				                                  arguments[paramindex + 1]);
+				// CHT: when a Chinese template is active, also translate the inserted
+				// %s string argument (e.g. the copy-protection question, or an
+				// inventory/location name dropped into a dynamic sentence) so the whole
+				// line renders in Chinese instead of a Chinese frame around English.
+				// A miss returns NULL and passes through unchanged, so a player-entered
+				// name (never in the table) stays verbatim.
+				if (chtArgCount >= 0) {
+					const char *argTr = g_sci->getChtTranslation(tempsource.c_str());
+					if (argTr)
+						tempsource = argTr;
+				}
 				int slen = tempsource.size();
 				int extralen = strLength - slen;
 				assert((target - targetbuf) + extralen <= maxsize);
diff --git a/engines/sci/event.cpp b/engines/sci/event.cpp
index 145d32f1..a78a0cc4 100644
--- a/engines/sci/event.cpp
+++ b/engines/sci/event.cpp
@@ -204,6 +204,18 @@ SciEvent EventManager::getScummVMEvent() {
 	bool found;
 	do {
 		found = em->pollEvent(ev);
+		// CHT: F8 toggles between the Chinese translation and the original English so players
+		// can compare wording. Consume it here rather than letting it reach the game — SCI
+		// scripts bind F8 themselves in some games. Only active while a translation is loaded,
+		// so English builds keep F8's original behaviour. The current text box is left alone
+		// (SCI ties box contents to transient port state); the toggle shows on the next line.
+		if (found && ev.type == Common::EVENT_KEYDOWN && ev.kbd.keycode == Common::KEYCODE_F8 &&
+		    g_sci->getLanguage() == Common::ZH_TWN) {
+			g_sci->toggleChtLang();
+			found = false;
+			ev.type = Common::EVENT_INVALID;
+			continue;
+		}
 	} while (found && ev.type == Common::EVENT_MOUSEMOVE);
 
 	Common::Point mousePos;
diff --git a/engines/sci/graphics/animate.cpp b/engines/sci/graphics/animate.cpp
index 5c29ebc5..72f51223 100644
--- a/engines/sci/graphics/animate.cpp
+++ b/engines/sci/graphics/animate.cpp
@@ -489,6 +489,12 @@ void GfxAnimate::updateScreen(byte oldPicNotValid) {
 	Common::Rect lsRect;
 	Common::Rect workerRect;
 
+	// CHT: the title screen's sparkle cels animate via kAnimate and draw over the visual
+	// plane every frame, covering the Chinese subtitle overlay. Re-assert the overlay onto
+	// the visual plane before the bitsShow loop below so it wins on screen (issue #1.3).
+	if (_paint16->_chtTitleActive)
+		_paint16->drawChtTitleOverlay();
+
 	for (it = _list.begin(); it != end; ++it) {
 		if (it->showBitsFlag || !(it->signal & (kSignalRemoveView | kSignalNoUpdate) ||
 										(!(it->signal & kSignalRemoveView) && (it->signal & kSignalNoUpdate) && oldPicNotValid))) {
diff --git a/engines/sci/graphics/cache.cpp b/engines/sci/graphics/cache.cpp
index fda7797e..b012b331 100644
--- a/engines/sci/graphics/cache.cpp
+++ b/engines/sci/graphics/cache.cpp
@@ -30,6 +30,7 @@
 #include "sci/graphics/scifont.h"
 #include "sci/graphics/fontsjis.h"
 #include "sci/graphics/fontkorean.h"
+#include "sci/graphics/fontchinese.h"
 #include "sci/graphics/view.h"
 
 namespace Sci {
@@ -72,6 +73,10 @@ GfxFont *GfxCache::getFont(GuiResourceId fontId) {
 		// Create special SJIS font in japanese games, when font 900 is selected
 		else if ((fontId == 900) && (g_sci->getLanguage() == Common::JA_JPN))
 			_cachedFonts[fontId] = new GfxFontSjis(_screen, fontId);
+		// Traditional Chinese (Big5) fan translation: wrap every font so ASCII
+		// stays on the original SCI glyphs and Big5 double-byte renders via Big5Font.
+		else if (g_sci->getLanguage() == Common::ZH_TWN)
+			_cachedFonts[fontId] = new GfxFontChinese(_resMan, _screen, fontId);
 		else
 			_cachedFonts[fontId] = new GfxFontFromResource(_resMan, _screen, fontId);
 	}
diff --git a/engines/sci/graphics/controls16.cpp b/engines/sci/graphics/controls16.cpp
index aac0b3d3..43d8a7f7 100644
--- a/engines/sci/graphics/controls16.cpp
+++ b/engines/sci/graphics/controls16.cpp
@@ -392,15 +392,25 @@ void GfxControls16::kernelDrawButton(Common::Rect rect, reg_t obj, const char *t
 void GfxControls16::kernelDrawText(Common::Rect rect, reg_t obj, const char *text, uint16 languageSplitter, int16 fontId, TextAlignment alignment, int16 style, bool hilite) {
 	g_sci->_tts->text(text);
 
+	// CHT: a control whose rect was measured for the English string needs to shift so the
+	// Chinese ends up where the English was (see GfxText16::chtControlOffset). The erase and
+	// the screen update have to cover both the old and the new position, or the part of the
+	// text that moved outside the original rect is drawn but never shown.
+	const int16 chtOffset = _text16->chtControlOffset(text, rect, fontId);
+	Common::Rect textRect(rect);
+	textRect.translate(chtOffset, 0);
+	Common::Rect updateRect(rect);
+	updateRect.extend(textRect);
+
 	if (!hilite) {
-		rect.grow(1);
-		_paint16->eraseRect(rect);
-		rect.grow(-1);
+		updateRect.grow(1);
+		_paint16->eraseRect(updateRect);
+		updateRect.grow(-1);
 		if (!g_sci->hasMacFonts()) {
 			// The PC-98 versions set the 'show` argument here (unlike normal DOS versions).
-			_text16->Box(text, languageSplitter, _screen->gfxDriver()->driverBasedTextRendering(), rect, alignment, fontId);
+			_text16->Box(text, languageSplitter, _screen->gfxDriver()->driverBasedTextRendering(), textRect, alignment, fontId);
 		} else {
-			_text16->macDraw(text, rect, alignment, fontId, _text16->GetFontId(), 0);
+			_text16->macDraw(text, textRect, alignment, fontId, _text16->GetFontId(), 0);
 		}
 		if (style & SCI_CONTROLS_STYLE_SELECTED) {
 			_paint16->frameRect(rect);
@@ -414,7 +424,7 @@ void GfxControls16::kernelDrawText(Common::Rect rect, reg_t obj, const char *tex
 		bool allowScreenUpdate = _screen->gfxDriver()->driverBasedTextRendering() ? false : true;
 
 		if (allowScreenUpdate && !getPicNotValid())
-			_paint16->bitsShow(rect);
+			_paint16->bitsShow(updateRect);
 	} else {
 		// SCI0early used xor to invert button rectangles resulting in pink/white buttons
 		// All PC-98 targets (both SCI_VERSION_01 and SCI_VERSION_1_LATE) also use the
diff --git a/engines/sci/graphics/menu.cpp b/engines/sci/graphics/menu.cpp
index cdabf3bf..a20d3c91 100644
--- a/engines/sci/graphics/menu.cpp
+++ b/engines/sci/graphics/menu.cpp
@@ -368,6 +368,9 @@ void GfxMenu::drawBar() {
 	else
 		_ports->moveTo(_screen->getWidth() - 8, 1);
 
+	// CHT: draw menu titles via the low-res Chinese path so highlight-invert works (see
+	// GfxScreen::setMenuTextActive).
+	_screen->setMenuTextActive(true);
 	listIterator = _list.begin();
 	while (listIterator != listEnd) {
 		GuiMenuEntry *listEntry = *listIterator;
@@ -384,6 +387,7 @@ void GfxMenu::drawBar() {
 
 		listIterator++;
 	}
+	_screen->setMenuTextActive(false);
 }
 
 // This helper calculates all text widths for all menus (only)
@@ -676,6 +680,9 @@ void GfxMenu::drawMenu(uint16 oldMenuId, uint16 newMenuId) {
 	else
 		_menuRect.right -= 8;
 	topPos = _menuRect.top + 1;
+	// CHT: draw dropdown items via the low-res Chinese path so highlight-invert works
+	// (see GfxScreen::setMenuTextActive). Reset after the loop below.
+	_screen->setMenuTextActive(true);
 	listItemIterator = _itemList.begin();
 	while (listItemIterator != listItemEnd) {
 		listItemEntry = *listItemIterator;
@@ -708,6 +715,7 @@ void GfxMenu::drawMenu(uint16 oldMenuId, uint16 newMenuId) {
 		listItemIterator++;
 	}
 	_ports->textGreyedOutput(false);
+	_screen->setMenuTextActive(false);
 
 	// Draw the black line again
 	_paint16->fillRect(_ports->_menuLine, 1, 0);
diff --git a/engines/sci/graphics/paint16.cpp b/engines/sci/graphics/paint16.cpp
index 2e901e8d..a2abbcf8 100644
--- a/engines/sci/graphics/paint16.cpp
+++ b/engines/sci/graphics/paint16.cpp
@@ -19,6 +19,8 @@
  *
  */
 
+#define FORBIDDEN_SYMBOL_EXCEPTION_getenv
+#include "common/file.h"
 #include "sci/sci.h"
 #include "sci/engine/features.h"
 #include "sci/engine/state.h"
@@ -47,7 +49,9 @@ GfxPaint16::GfxPaint16(ResourceManager *resMan, SegManager *segMan, GfxCache *ca
 	: _resMan(resMan), _segMan(segMan), _cache(cache), _ports(ports),
 	  _coordAdjuster(coordAdjuster), _screen(screen), _palette(palette),
 	  _transitions(transitions), _audio(audio), _EGAdrawingVisualize(false),
-	  _hiresDrawObjs(nullptr), _hiresPortraitWorkaroundFlag(false) {
+	  _hiresDrawObjs(nullptr), _hiresPortraitWorkaroundFlag(false),
+	  _chtTitleActive(false), _chtTitleLoaded(false),
+	  _chtTitleW(0), _chtTitleH(0), _chtTitleX(0), _chtTitleY(0) {
 
 	// _animate and _text16 will be initialized later on
 	_animate = nullptr;
@@ -88,6 +92,8 @@ void GfxPaint16::debugSetEGAdrawingVisualize(bool state) {
 }
 
 void GfxPaint16::drawPicture(GuiResourceId pictureId, bool mirroredFlag, bool addToFlag, GuiResourceId paletteId) {
+	if (getenv("SCI_LOG_GFX"))
+		warning("SCI_LOG_GFX drawPicture pic=%d", pictureId);
 	// Set up custom per-picture palette mod
 	doCustomPicPalette(_screen, pictureId);
 
@@ -106,6 +112,115 @@ void GfxPaint16::drawPicture(GuiResourceId pictureId, bool mirroredFlag, bool ad
 
 	// Reset custom per-picture palette mod
 	_screen->setCurPaletteMapValue(0);
+
+	// CHT: overlay a Traditional Chinese subtitle onto the KQ1 title screen (pic 777, the first
+	// picture the game draws — confirmed with SCI_LOG_GFX). The "King's Quest 1 / Quest for the
+	// Crown" logo is vector pic art (not a bitmap we can edit), so we blit an indexed EGA
+	// overlay onto the visual plane (see drawChtTitleOverlay). Title sparkle cels animate on
+	// top every frame, so we keep _chtTitleActive and re-assert the overlay in drawCelAndShow.
+	const bool chtTitleWasActive = _chtTitleActive;
+	// Follows the F8 language toggle too: showing a Chinese title over an otherwise English
+	// screen would be inconsistent.
+	if (g_sci->getLanguage() == Common::ZH_TWN && g_sci->chtLangOn()) {
+		if (pictureId == 777)
+			_chtTitleActive = true;      // title screen drawn
+		else if (pictureId != 902)
+			_chtTitleActive = false;     // a real room picture replaced it
+		// pic 902 is drawn on top of the title while its menu is up, so it must re-assert
+		// rather than cancel the overlay (otherwise the subtitle vanishes a frame later).
+	}
+	if (_chtTitleActive)
+		drawChtTitleOverlay();
+	else if (chtTitleWasActive)
+		clearChtTitleOverlay();
+
+	// CHT tooling: dump each drawn pic (display buffer + palette) as PPM, to identify
+	// which resource holds baked-art UI labels. Triggered by SCI_DUMP_PIC=<dir>.
+	if (const char *dir = getenv("SCI_DUMP_PIC")) {
+		const int w = _screen->getDisplayWidth(), h = _screen->getDisplayHeight();
+		const byte *buf = _screen->getDisplayBuffer();
+		const Palette &pal = _palette->_sysPalette;
+		Common::String fn = Common::String::format("%s/pic_%d.ppm", dir, pictureId);
+		Common::DumpFile out;
+		if (out.open(Common::Path(fn))) {
+			Common::String hdr = Common::String::format("P6\n%d %d\n255\n", w, h);
+			out.write(hdr.c_str(), hdr.size());
+			for (int i = 0; i < w * h; i++) {
+				const Color &c = pal.colors[buf[i]];
+				byte rgb[3] = { c.r, c.g, c.b };
+				out.write(rgb, 3);
+			}
+			out.finalize();
+			out.close();
+		}
+	}
+}
+
+// CHT: blit the cached Traditional Chinese title subtitle (kq1_title.ovl) onto the visual
+// plane. Loaded once (indexed EGA bitmap: LE u16 w,h,x,y in logical 320x200 coords, then
+// w*h bytes of EGA index; 0xFF = transparent). putPixel writes the visual plane (persists
+// across re-upscale) and 2x-upscales to the display; low-res matches the blocky EGA art.
+void GfxPaint16::drawChtTitleOverlay() {
+	if (!_chtTitleLoaded) {
+		_chtTitleLoaded = true; // one attempt; on failure stay empty and no-op
+		Common::File ovl;
+		if (ovl.open("kq1_title.ovl")) {
+			_chtTitleW = ovl.readUint16LE();
+			_chtTitleH = ovl.readUint16LE();
+			_chtTitleX = ovl.readUint16LE();
+			_chtTitleY = ovl.readUint16LE();
+			uint32 n = (uint32)_chtTitleW * _chtTitleH;
+			_chtTitleData.resize(n);
+			for (uint32 i = 0; i < n && !ovl.eos(); i++)
+				_chtTitleData[i] = ovl.readByte();
+			ovl.close();
+		}
+	}
+	if (getenv("SCI_LOG_GFX"))
+		warning("SCI_LOG_GFX chtTitle blit: %ux%u at (%u,%u), %u bytes", _chtTitleW, _chtTitleH,
+		        _chtTitleX, _chtTitleY, (unsigned)_chtTitleData.size());
+	if (_chtTitleData.empty())
+		return;
+	const int sw = _screen->getWidth();
+	const int sh = _screen->getHeight();
+	uint32 i = 0;
+	for (uint16 yy = 0; yy < _chtTitleH; yy++) {
+		for (uint16 xx = 0; xx < _chtTitleW; xx++, i++) {
+			byte idx = _chtTitleData[i];
+			int px = _chtTitleX + xx, py = _chtTitleY + yy;
+			if (idx != 0xFF && px >= 0 && py >= 0 && px < sw && py < sh)
+				_screen->putPixel(px, py, GFX_SCREEN_MASK_VISUAL, idx, 0, 0);
+		}
+	}
+	// Push the overlay area to the screen ourselves. Writing the visual plane alone is not
+	// enough here: the title screen only ever shows the small rects its sparkle cels animate
+	// in, so a subtitle drawn outside those rects would never reach the display.
+	Common::Rect r(_chtTitleX, _chtTitleY, _chtTitleX + _chtTitleW, _chtTitleY + _chtTitleH);
+	r.clip(Common::Rect(sw, sh));
+	if (!r.isEmpty())
+		_screen->copyRectToScreen(r);
+}
+
+// CHT: wipe the title subtitle when leaving the title screen. The overlay sits in the top
+// rows of the visual plane, and a room picture only repaints from the status bar down, so
+// without this the Chinese title bleeds into the first scene as a clipped red smear.
+void GfxPaint16::clearChtTitleOverlay() {
+	if (_chtTitleData.empty())
+		return;
+	const int sw = _screen->getWidth();
+	const int sh = _screen->getHeight();
+	uint32 i = 0;
+	for (uint16 yy = 0; yy < _chtTitleH; yy++) {
+		for (uint16 xx = 0; xx < _chtTitleW; xx++, i++) {
+			int px = _chtTitleX + xx, py = _chtTitleY + yy;
+			if (_chtTitleData[i] != 0xFF && px >= 0 && py >= 0 && px < sw && py < sh)
+				_screen->putPixel(px, py, GFX_SCREEN_MASK_VISUAL, 0, 0, 0);
+		}
+	}
+	Common::Rect r(_chtTitleX, _chtTitleY, _chtTitleX + _chtTitleW, _chtTitleY + _chtTitleH);
+	r.clip(Common::Rect(sw, sh));
+	if (!r.isEmpty())
+		_screen->copyRectToScreen(r);
 }
 
 // This one is the only one that updates screen!
@@ -121,6 +236,11 @@ void GfxPaint16::drawCelAndShow(GuiResourceId viewId, int16 loopNo, int16 celNo,
 
 		drawCel(view, loopNo, celNo, celRect, priority, paletteNo, scaleX, scaleY, scaleSignal);
 
+		// CHT: a title-screen sparkle cel just drew over the visual plane; re-assert the
+		// Chinese subtitle before bitsShow so the animation doesn't cover it (issue #1.3).
+		if (_chtTitleActive)
+			drawChtTitleOverlay();
+
 		if (getSciVersion() >= SCI_VERSION_1_1) {
 			if (!_screen->_picNotValidSci11) {
 				bitsShow(celRect);
@@ -565,6 +685,16 @@ reg_t GfxPaint16::kernelDisplay(const char *text, uint16 languageSplitter, int a
 		rect.moveTo(_ports->getPort()->curLeft, _ports->getPort()->curTop);
 	}
 
+	// CHT: kDisplay sizes the box from the text and then drops it at the cursor position the
+	// script set, and that position was chosen for the English string. KQ1's title screen is
+	// the visible case: the four entries sit on blue plates painted into the picture, and the
+	// script moves to the x where the English label starts so it lands centred on its plate.
+	// "Credits" is 31px wide, 製作名單 is 64px, same left edge — the Chinese hangs off the
+	// right of the plate. Shifting the whole rect keeps the fill, the draw and the screen
+	// update in agreement; chtControlOffset() returns 0 for anything that is not a box built
+	// to fit its own text.
+	rect.translate(_text16->chtControlOffset(text, rect, -1), 0);
+
 	// Kawa's SCI11+
 	if (stroke)
 		rect.grow(1);
diff --git a/engines/sci/graphics/paint16.h b/engines/sci/graphics/paint16.h
index d2421f0b..616796a3 100644
--- a/engines/sci/graphics/paint16.h
+++ b/engines/sci/graphics/paint16.h
@@ -45,6 +45,14 @@ public:
 
 	void drawPicture(GuiResourceId pictureId, bool mirroredFlag, bool addToFlag, GuiResourceId paletteId);
 	void drawCelAndShow(GuiResourceId viewId, int16 loopNo, int16 celNo, uint16 leftPos, uint16 topPos, byte priority, uint16 paletteNo, uint16 scaleX = 128, uint16 scaleY = 128, uint16 scaleSignal = 0);
+
+	// CHT: Traditional Chinese title-screen subtitle overlay (kq1_title.ovl), blitted to the
+	// visual plane. Cached and re-asserted over the title's animated sparkle cels each frame
+	// (drawCelAndShow + GfxAnimate::updateScreen) so they don't cover it. Public so GfxAnimate
+	// can re-assert it before its own bitsShow. Active only while the title pic (96) is shown.
+	void drawChtTitleOverlay();
+	void clearChtTitleOverlay();
+	bool _chtTitleActive;
 	void drawCel(GuiResourceId viewId, int16 loopNo, int16 celNo, const Common::Rect &celRect, byte priority, uint16 paletteNo, uint16 scaleX = 128, uint16 scaleY = 128, uint16 scaleSignal = 0);
 	void drawCel(GfxView *view, int16 loopNo, int16 celNo, const Common::Rect &celRect, byte priority, uint16 paletteNo, uint16 scaleX = 128, uint16 scaleY = 128, uint16 scaleSignal = 0);
 	void drawHiresCelAndShow(GuiResourceId viewId, int16 loopNo, int16 celNo, uint16 leftPos, uint16 topPos, byte priority, uint16 paletteNo, reg_t hiresHandle, bool storeDrawingInfo);
@@ -109,6 +117,11 @@ private:
 
 	HiresDrawData *_hiresDrawObjs;
 	bool _hiresPortraitWorkaroundFlag;
+
+	// CHT: title overlay pixel cache + one-shot load state (see public drawChtTitleOverlay).
+	bool _chtTitleLoaded;
+	uint16 _chtTitleW, _chtTitleH, _chtTitleX, _chtTitleY;
+	Common::Array<byte> _chtTitleData;
 };
 
 } // End of namespace Sci
diff --git a/engines/sci/graphics/ports.cpp b/engines/sci/graphics/ports.cpp
index 1bfe51af..b412631c 100644
--- a/engines/sci/graphics/ports.cpp
+++ b/engines/sci/graphics/ports.cpp
@@ -62,9 +62,14 @@ void GfxPorts::init(bool usesOldGfxFunctions, GfxPaint16 *paint16, GfxText16 *te
 	setPort(_menuPort);
 	_text16->SetFont(0);
 	_menuPort->rect = Common::Rect(0, 0, _screen->getScriptWidth(), _screen->getScriptHeight());
-	_menuBarRect = Common::Rect(0, 0, _screen->getScriptWidth(), 9);
-	_menuRect = Common::Rect(0, 0, _screen->getScriptWidth(), 10);
-	_menuLine = Common::Rect(0, 9, _screen->getScriptWidth(), 10);
+	// CHT: the stock bar is 9 rows, which is enough for the 8px English font and cuts the
+	// bottom half off every 15px Big5 glyph — menu titles and the status line both. The bar
+	// and the status line are filled from this same rect (GfxMenu::drawBar,
+	// GfxMenu::kernelDrawStatus), so heightening it moves the fill with the text.
+	const int16 menuBarH = (g_sci->getLanguage() == Common::ZH_TWN) ? 15 : 9;
+	_menuBarRect = Common::Rect(0, 0, _screen->getScriptWidth(), menuBarH);
+	_menuRect = Common::Rect(0, 0, _screen->getScriptWidth(), menuBarH + 1);
+	_menuLine = Common::Rect(0, menuBarH, _screen->getScriptWidth(), menuBarH + 1);
 
 	_wmgrPort = new Port(1);
 	_windowsById.resize(2);
diff --git a/engines/sci/graphics/screen.cpp b/engines/sci/graphics/screen.cpp
index 308fa82c..449fb126 100644
--- a/engines/sci/graphics/screen.cpp
+++ b/engines/sci/graphics/screen.cpp
@@ -84,6 +84,13 @@ GfxScreen::GfxScreen(ResourceManager *resMan, Common::RenderMode renderMode) : _
 		}
 	}
 
+	// CHT note (KQ1): other SCI0 fan translations (KQ4, LSL2) force GFX_SCREEN_UPSCALED_640x400
+	// for ZH_TWN so Big5 glyphs can be drawn with real 24x24 detail. KQ1 must NOT do that: with
+	// the forced upscale its permanent status bar renders wrong (left half turns black, text
+	// clipped) — and it breaks the same way with plain English text, so it is the upscale
+	// itself that KQ1's status-bar drawing does not survive, not anything Chinese. KQ1 renders
+	// Chinese with the native 16x15 ETEN glyphs at 320x200 instead, which also matches how the
+	// AGI track looks.
 	// if not yet set, set those to script-width/height
 	if (!_width)
 		_width = _scriptWidth;
diff --git a/engines/sci/graphics/screen.h b/engines/sci/graphics/screen.h
index eaed025a..7bbdb648 100644
--- a/engines/sci/graphics/screen.h
+++ b/engines/sci/graphics/screen.h
@@ -78,6 +78,7 @@ public:
 	uint16 getScriptHeight() { return _scriptHeight; }
 	uint16 getDisplayWidth() { return _displayWidth; }
 	uint16 getDisplayHeight() { return _displayHeight; }
+	const byte *getDisplayBuffer() { return _displayScreen; } // CHT tooling: pic dump
 	byte getColorWhite() { return _colorWhite; }
 	byte getColorDefaultVectorData() { return _colorDefaultVectorData; }
 
@@ -151,6 +152,15 @@ public:
 	void setFontIsUpscaled(bool isUpscaled) { _fontIsUpscaled = isUpscaled; }
 	bool fontIsUpscaled() const { return _fontIsUpscaled; }
 
+	// CHT: while a menu bar/dropdown is being drawn, force Chinese glyphs down the low-res
+	// path so they land in the visual buffer and thus invert correctly on highlight. The
+	// hi-res path writes straight to the display buffer, which invertRect (visual only) +
+	// bitsShow (re-upscale) would wipe to a black-on-black (invisible) selection.
+	void setMenuTextActive(bool active) { _menuTextActive = active; }
+	bool menuTextActive() const { return _menuTextActive; }
+	void setCompactTextActive(bool active) { _compactTextActive = active; }
+	bool compactTextActive() const { return _compactTextActive; }
+
 	void grabPalette(byte *buffer, uint start, uint num) const;
 	void setPalette(const byte *buffer, uint start, uint num, bool update = true);
 
@@ -249,6 +259,10 @@ private:
 	 * to the screen size (and we therefore should not upscale it ourselves).
 	 */
 	bool _fontIsUpscaled;
+	bool _menuTextActive = false;
+	// CHT: true while the status bar draws — it is only 10 script rows tall, so Chinese must
+	// use the compact 16x15 glyph instead of the 24px hi-res one or it gets clipped.
+	bool _compactTextActive = false;
 
 
 	// pixel related code, in header so that it can be inlined for performance
diff --git a/engines/sci/graphics/text16.cpp b/engines/sci/graphics/text16.cpp
index 9e5647f3..4717a3cd 100644
--- a/engines/sci/graphics/text16.cpp
+++ b/engines/sci/graphics/text16.cpp
@@ -19,6 +19,9 @@
  *
  */
 
+// CHT tooling: SCI_CHT_DEBUG prints the text/rect the engine is about to lay out.
+#define FORBIDDEN_SYMBOL_EXCEPTION_getenv
+#include "common/file.h"
 #include "common/util.h"
 #include "common/stack.h"
 #include "common/unicode-bidi.h"
@@ -273,7 +276,14 @@ int16 GfxText16::GetLongest(const char *&textPtr, int16 maxWidth, GuiResourceId
 
 		// the previous greater than test was originally a greater than or equals when
 		//  no space character had been reached yet
-		if (_useEarlyGetLongestTextCalculations) {
+		// SCI0's original interpreter broke a spaceless run one character early when it exactly
+		// filled the line. That is harmless for English (a spaceless run only happens mid-word),
+		// but Chinese has no spaces at all, so the condition fires on every line that fits
+		// exactly — and the game sizes its own controls to the measured text width, so "fits
+		// exactly" is the normal case: every button lost its last character (簡介 -> 簡,
+		// 開始遊戲 -> 開始遊). Keep the quirk for single-byte text, skip it for Big5.
+		if (_useEarlyGetLongestTextCalculations &&
+		    !(g_sci->getLanguage() == Common::ZH_TWN && curChar > 0xFF)) {
 			if (lastSpaceCharCount == 0 && tempWidth == maxWidth) {
 				break;
 			}
@@ -303,7 +313,21 @@ int16 GfxText16::GetLongest(const char *&textPtr, int16 maxWidth, GuiResourceId
 		// Break without spaces found, we split the very first word - may also be Kanji/Japanese
 
 		if (curChar > 0xFF) {
-			// current character is Japanese
+			// current character is Japanese (or Traditional Chinese Big5)
+
+			// Traditional Chinese (Big5) fan translation: wrap a spaceless run at the
+			// last character that fits. The PC-98 SJIS kinsoku logic below intentionally
+			// over-packs the line by one double-byte char (and does punctuation seek-back);
+			// for centered Big5 text that makes textWidth exceed the box, so Box()'s
+			// CENTER offset = (width - textWidth)/2 goes negative and the first char of the
+			// line is pushed left of the box, clipping its left radical (你->尔, 據->豦).
+			// For ZH_TWN we just keep the characters that already fit within maxWidth.
+			if (g_sci->getLanguage() == Common::ZH_TWN) {
+				if (curCharCount == 0) {
+					// Guarantee forward progress if not even one glyph fits.
+					curCharCount += 2; textPtr += 2;
+				}
+			} else {
 
 			// PC-9801 SCI actually added the last character, which shouldn't fit anymore, still onto the
 			//  screen in case maxWidth wasn't fully reached with the last character
@@ -360,6 +384,7 @@ int16 GfxText16::GetLongest(const char *&textPtr, int16 maxWidth, GuiResourceId
 				// (game mentions Mixed Up Fairy Tales and uses English letters for that)
 				textPtr += 2;
 			}
+			} // end non-ZH_TWN PC-98 SJIS kinsoku branch
 		} else {
 			// Add a character to the count for games whose interpreter would count the
 			//  character that exceeded the width if a space hadn't been reached yet.
@@ -432,6 +457,16 @@ void GfxText16::Width(const char *text, int16 from, int16 len, GuiResourceId org
 }
 
 void GfxText16::StringWidth(const Common::String &str, GuiResourceId orgFontId, int16 &textWidth, int16 &textHeight) {
+	// CHT: measure the translated string so menu/UI widths match what DrawString actually
+	// draws (e.g. the Chinese menu bar items). Without this the box is sized for the English
+	// text and the Chinese overflows or is clipped.
+	if (g_sci->getLanguage() == Common::ZH_TWN) {
+		const char *tr = g_sci->getChtTranslation(str.c_str());
+		if (tr) {
+			Width(tr, 0, strlen(tr), orgFontId, textWidth, textHeight, true);
+			return;
+		}
+	}
 	Width(str.c_str(), 0, str.size(), orgFontId, textWidth, textHeight, true);
 }
 
@@ -441,7 +476,48 @@ void GfxText16::ShowString(const Common::String &str, GuiResourceId orgFontId, i
 }
 #endif
 
+// CHT: how far a text control has to move so the Chinese sits where the English sat.
+//
+// KQ1's title screen hard-codes rect.left for each of its four entries and sizes the rect from
+// kTextSize, so the box always hugs the text: "Credits" is 31px wide starting at x=84, and the
+// blue plate behind it is part of the picture, centred on where those 31px land. 製作名單 is
+// 64px, keeps the same left edge, and so hangs 33px off the right of its plate.
+//
+// Only a rect that hugs its text is moved — a dialogue box is wider than the string inside it,
+// so the width test below leaves it alone. Vertical placement is left as it is: by this point
+// the port's font height is already the Chinese one, so the English height is not recoverable,
+// and a 15px glyph on a plate drawn for a 9px font overflows it whatever we do.
+int16 GfxText16::chtControlOffset(const char *text, const Common::Rect &rect, GuiResourceId fontId) {
+	if (g_sci->getLanguage() != Common::ZH_TWN || !text)
+		return 0;
+	const char *translated = g_sci->getChtTranslation(text);
+	if (!translated)
+		return 0;
+
+	GuiResourceId previousFontId = GetFontId();
+	if (fontId != -1)
+		SetFont(fontId);
+	int16 englishWidth = 0, chineseWidth = 0, unusedHeight = 0;
+	Width(text, 0, strlen(text), fontId, englishWidth, unusedHeight, true);
+	Width(translated, 0, strlen(translated), fontId, chineseWidth, unusedHeight, true);
+	SetFont(previousFontId);
+
+	if (getenv("SCI_CHT_DEBUG"))
+		warning("KQ1DBG chtControlOffset text='%s' rectW=%d enW=%d zhW=%d", text, rect.width(), englishWidth, chineseWidth);
+	if (englishWidth <= 0 || chineseWidth <= 0 || rect.width() != chineseWidth)
+		return 0;
+	return (englishWidth - chineseWidth) / 2;
+}
+
 void GfxText16::DrawString(const Common::String &str, GuiResourceId orgFontId, int16 orgPenColor) {
+	// Traditional Chinese fan translation (content-keyed).
+	if (g_sci->getLanguage() == Common::ZH_TWN) {
+		const char *tr = g_sci->getChtTranslation(str.c_str());
+		if (tr) {
+			Draw(tr, 0, strlen(tr), orgFontId, orgPenColor);
+			return;
+		}
+	}
 	Draw(str.c_str(), 0, str.size(), orgFontId, orgPenColor);
 }
 
@@ -569,6 +645,19 @@ void GfxText16::Box(const char *text, uint16 languageSplitter, bool show, const
 	int16 textWidth, maxTextWidth, textHeight;
 	int16 offset = 0;
 	int16 hline = 0;
+
+	// Traditional Chinese fan translation: swap the original English string for
+	// its Big5 translation (content-keyed). GfxFontChinese renders the double-byte glyphs.
+	if (g_sci->getLanguage() == Common::ZH_TWN) {
+		const char *tr = g_sci->getChtTranslation(text);
+		if (tr)
+			text = tr;
+	}
+
+	if (getenv("SCI_CHT_DEBUG"))
+		warning("CHT Box rect=(%d,%d,%d,%d) w=%d h=%d align=%d text='%s'", rect.left, rect.top,
+		        rect.right, rect.bottom, rect.width(), rect.height(), alignment, text);
+
 	GuiResourceId previousFontId = GetFontId();
 	int16 previousPenColor = _ports->_curPort->penClr;
 	bool doubleByteMode = false;
@@ -683,10 +772,18 @@ void GfxText16::Box(const char *text, uint16 languageSplitter, bool show, const
 	_ports->penColor(previousPenColor);
 }
 
-void GfxText16::DrawString(const Common::String &textOrig) {
+void GfxText16::DrawString(const Common::String &textOrig0) {
 	GuiResourceId previousFontId = GetFontId();
 	int16 previousPenColor = _ports->_curPort->penClr;
 
+	// Traditional Chinese fan translation (content-keyed).
+	Common::String textOrig = textOrig0;
+	if (g_sci->getLanguage() == Common::ZH_TWN) {
+		const char *tr = g_sci->getChtTranslation(textOrig0.c_str());
+		if (tr)
+			textOrig = tr;
+	}
+
 	Common::String text;
 	if (!g_sci->isLanguageRTL())
 		text = textOrig;
@@ -700,11 +797,19 @@ void GfxText16::DrawString(const Common::String &textOrig) {
 
 // we need to have a separate status drawing code
 //  In KQ4 the IV char is actually 0xA, which would otherwise get considered as linebreak and not printed
-void GfxText16::DrawStatus(const Common::String &strOrig) {
+void GfxText16::DrawStatus(const Common::String &strOrig0) {
 	GetFont();
 	if (!_font)
 		return;
 
+	// Traditional Chinese fan translation (content-keyed).
+	Common::String strOrig = strOrig0;
+	if (g_sci->getLanguage() == Common::ZH_TWN) {
+		const char *tr = g_sci->getChtTranslation(strOrig0.c_str());
+		if (tr)
+			strOrig = tr;
+	}
+
 	Common::String str;
 	if (!g_sci->isLanguageRTL())
 		str = strOrig;
@@ -717,8 +822,18 @@ void GfxText16::DrawStatus(const Common::String &strOrig) {
 
 	rect.top = _ports->_curPort->curTop;
 	rect.bottom = rect.top + _ports->_curPort->fontHeight;
+	// CHT: the status bar is only 10 script rows (20 display rows) tall, so a 24px hi-res
+	// glyph overflows it — tell GfxFontChinese to use its compact 16x15 glyph here.
+	_screen->setCompactTextActive(true);
 	while (textLen--) {
 		uint16 curChar = *text++;
+		// The status bar draws byte by byte, which turns a Big5 pair into two garbage Latin
+		// glyphs (the score line rendered as "G0 0158"). Combine the pair the same way Box()
+		// and DrawString() do so GfxFontChinese sees one character.
+		if (textLen && _font->isDoubleByte(curChar)) {
+			curChar |= (*text++) << 8;
+			textLen--;
+		}
 		switch (curChar) {
 		case 0:
 			break;
@@ -729,6 +844,7 @@ void GfxText16::DrawStatus(const Common::String &strOrig) {
 		}
 		}
 	}
+	_screen->setCompactTextActive(false);
 }
 
 // Check for Korean strings, and use font 1001 to render them
diff --git a/engines/sci/graphics/text16.h b/engines/sci/graphics/text16.h
index 5dbd4106..ac40f73a 100644
--- a/engines/sci/graphics/text16.h
+++ b/engines/sci/graphics/text16.h
@@ -60,6 +60,7 @@ public:
 	int16 GetLongest(const char *&text, int16 maxWidth, GuiResourceId orgFontId);
 	void Width(const char *text, int16 from, int16 len, GuiResourceId orgFontId, int16 &textWidth, int16 &textHeight, bool restoreFont);
 	void StringWidth(const Common::String &str, GuiResourceId orgFontId, int16 &textWidth, int16 &textHeight);
+	int16 chtControlOffset(const char *text, const Common::Rect &rect, GuiResourceId fontId);
 #if 0
 	void ShowString(const Common::String &str, GuiResourceId orgFontId, int16 orgPenColor);
 #endif
diff --git a/engines/sci/graphics/view.cpp b/engines/sci/graphics/view.cpp
index 3bc1bc7c..58d346d7 100644
--- a/engines/sci/graphics/view.cpp
+++ b/engines/sci/graphics/view.cpp
@@ -19,6 +19,8 @@
  *
  */
 
+#define FORBIDDEN_SYMBOL_EXCEPTION_getenv
+#include "common/file.h"
 #include "sci/sci.h"
 #include "sci/engine/state.h"
 #include "sci/graphics/drivers/gfxdriver.h"
@@ -791,6 +793,32 @@ byte GfxView::getMappedColor(byte color, uint16 scaleSignal, const Palette *pale
 	return outputColor;
 }
 
+void GfxView::dumpCelsToDir(const char *dir) {
+	const Palette *palette = _embeddedPal ? &_viewPalette : &_palette->_sysPalette;
+	for (int16 loopNo = 0; loopNo < (int16)getLoopCount(); loopNo++) {
+		for (int16 celNo = 0; celNo < (int16)getCelCount(loopNo); celNo++) {
+			const CelInfo *celInfo = getCelInfo(loopNo, celNo);
+			const SciSpan<const byte> &bitmap = getBitmap(loopNo, celNo);
+			const int16 w = celInfo->width, h = celInfo->height;
+			if (w <= 0 || h <= 0)
+				continue;
+			Common::String fn = Common::String::format("%s/view_%d_%d_%d.ppm", dir, _resourceId, loopNo, celNo);
+			Common::DumpFile out;
+			if (!out.open(Common::Path(fn)))
+				continue;
+			Common::String hdr = Common::String::format("P6\n%d %d\n255\n", w, h);
+			out.write(hdr.c_str(), hdr.size());
+			for (int i = 0; i < w * h; i++) {
+				const Color &c = palette->colors[bitmap[i]];
+				byte rgb[3] = { c.r, c.g, c.b };
+				out.write(rgb, 3);
+			}
+			out.finalize();
+			out.close();
+		}
+	}
+}
+
 void GfxView::draw(const Common::Rect &rect, const Common::Rect &clipRect, const Common::Rect &clipRectTranslated,
 			int16 loopNo, int16 celNo, byte priority, uint16 EGAmappingNr, bool upscaledHires, uint16 scaleSignal) {
 	const Palette *palette = _embeddedPal ? &_viewPalette : &_palette->_sysPalette;
@@ -799,6 +827,25 @@ void GfxView::draw(const Common::Rect &rect, const Common::Rect &clipRect, const
 	const int16 celHeight = celInfo->height;
 	const int16 celWidth = celInfo->width;
 	const byte clearKey = celInfo->clearKey;
+
+	// CHT tooling: identify/extract baked-art view cels.
+	if (getenv("SCI_LOG_GFX"))
+		warning("SCI_LOG_GFX view=%d loop=%d cel=%d %dx%d @(%d,%d)", _resourceId, loopNo, celNo, celWidth, celHeight, rect.left, rect.top);
+	if (const char *vdir = getenv("SCI_DUMP_VIEW")) {
+		Common::String fn = Common::String::format("%s/view_%d_%d_%d.ppm", vdir, _resourceId, loopNo, celNo);
+		Common::DumpFile vout;
+		if (vout.open(Common::Path(fn))) {
+			Common::String hdr = Common::String::format("P6\n%d %d\n255\n", celWidth, celHeight);
+			vout.write(hdr.c_str(), hdr.size());
+			for (int i = 0; i < celWidth * celHeight; i++) {
+				const Color &c = palette->colors[bitmap[i]];
+				byte rgb[3] = { c.r, c.g, c.b };
+				vout.write(rgb, 3);
+			}
+			vout.finalize();
+			vout.close();
+		}
+	}
 	const byte drawMask = priority > 15 ? GFX_SCREEN_MASK_VISUAL : GFX_SCREEN_MASK_VISUAL|GFX_SCREEN_MASK_PRIORITY;
 
 	if (_embeddedPal)
diff --git a/engines/sci/graphics/view.h b/engines/sci/graphics/view.h
index 64f20a18..797f9b51 100644
--- a/engines/sci/graphics/view.h
+++ b/engines/sci/graphics/view.h
@@ -75,6 +75,9 @@ public:
 	uint16 getCelCount(int16 loopNo) const;
 	Palette *getPalette();
 
+	// CHT tooling: dump every cel of this view as PPM (identify baked-art labels).
+	void dumpCelsToDir(const char *dir);
+
 	bool isScaleable();
 
 	void adjustToUpscaledCoordinates(int16 &y, int16 &x);
diff --git a/engines/sci/module.mk b/engines/sci/module.mk
index 94d96a6e..b4c689eb 100644
--- a/engines/sci/module.mk
+++ b/engines/sci/module.mk
@@ -48,6 +48,7 @@ MODULE_OBJS := \
 	graphics/controls16.o \
 	graphics/coordadjuster.o \
 	graphics/cursor.o \
+	graphics/fontchinese.o \
 	graphics/fontkorean.o \
 	graphics/fontsjis.o \
 	graphics/macfont.o \
diff --git a/engines/sci/sci.cpp b/engines/sci/sci.cpp
index b4d28b79..9d326051 100644
--- a/engines/sci/sci.cpp
+++ b/engines/sci/sci.cpp
@@ -19,6 +19,9 @@
  *
  */
 
+// CHT tooling hook (SCI_DUMP_RES) needs getenv; allow it for this file only.
+#define FORBIDDEN_SYMBOL_EXCEPTION_getenv
+
 #include "common/system.h"
 #include "common/config-manager.h"
 #include "common/debug-channels.h"
@@ -50,6 +53,7 @@
 #include "sci/sound/soundcmd.h"
 #include "sci/graphics/animate.h"
 #include "sci/graphics/cache.h"
+#include "sci/graphics/view.h"
 #include "sci/graphics/compare.h"
 #include "sci/graphics/controls16.h"
 #include "sci/graphics/coordadjuster.h"
@@ -289,6 +293,34 @@ Common::Error SciEngine::run() {
 	}
 */
 
+	// CHT tooling: dump decompressed text/message/script resources as patch files
+	// (uses ScummVM's own SCI decompressor). Triggered by SCI_DUMP_RES=<dir>; quits after.
+	if (const char *dumpDir = getenv("SCI_DUMP_RES")) {
+		const ResourceType dumpTypes[] = { kResourceTypeText, kResourceTypeMessage, kResourceTypeFont, kResourceTypeScript, kResourceTypeHeap, kResourceTypeView, kResourceTypePic };
+		for (int ti = 0; ti < 7; ++ti) {
+			Common::List<ResourceId> list = _resMan->listResources(dumpTypes[ti], -1);
+			for (Common::List<ResourceId>::iterator it = list.begin(); it != list.end(); ++it) {
+				Resource *r = _resMan->findResource(*it, 0);
+				if (!r)
+					continue;
+				Common::String fn = Common::String::format("%s/%s.%03d", dumpDir,
+					getResourceTypeName(dumpTypes[ti]), it->getNumber());
+				Common::DumpFile out;
+				if (out.open(Common::Path(fn))) {
+					r->writeToStream(&out);
+					out.finalize();
+					out.close();
+				}
+			}
+		}
+		warning("SCI_DUMP_RES: dumped text/message/font resources to %s", dumpDir);
+		return Common::kNoError;
+	}
+
+	// Traditional Chinese fan translation: load the content-keyed translation table.
+	if (getLanguage() == Common::ZH_TWN)
+		loadChtTranslation();
+
 	// Add the after market patches for the specified game, if they exist
 	_resMan->addNewGMPatch(_gameId);
 	_resMan->addNewD110Patch(_gameId);
@@ -585,6 +617,19 @@ void SciEngine::initGraphics() {
 
 	_gfxCache = new GfxCache(_resMan, _gfxScreen, _gfxPalette16);
 
+	// CHT tooling: dump every VIEW resource's cels as PPM (deterministic, no navigation),
+	// to find baked-art UI labels. Triggered by SCI_DUMP_ALLVIEWS=<dir>; quits after.
+	if (const char *vdir = getenv("SCI_DUMP_ALLVIEWS")) {
+		Common::List<ResourceId> views = _resMan->listResources(kResourceTypeView, -1);
+		for (Common::List<ResourceId>::iterator it = views.begin(); it != views.end(); ++it) {
+			GfxView *v = new GfxView(_resMan, _gfxScreen, _gfxPalette16, it->getNumber());
+			v->dumpCelsToDir(vdir);
+			delete v;
+		}
+		warning("SCI_DUMP_ALLVIEWS: dumped all view cels to %s", vdir);
+		return; // dump done; files flushed. initGraphics is void, so just return.
+	}
+
 #ifdef ENABLE_SCI32
 	if (getSciVersion() >= SCI_VERSION_2) {
 		// SCI32 graphic objects creation
@@ -831,7 +876,105 @@ const char *SciEngine::getGameIdStr() const {
 	return _gameDescription->gameId;
 }
 
+// CHT: collapse every run of whitespace (space, \r, \n, \t) to a single space and trim
+// the ends. SCI message resources embed hard "\r\n" line breaks, but translation.tsv keys
+// are single-line with those breaks flattened to spaces -- without this the runtime string
+// (e.g. the kFormat money template "...piece%s,\r\n weighing...") never matches its key.
+// Applied identically to keys at load and to lookups, so an already-matching pair still
+// matches (both collapse the same way).
+static Common::String sciChtNormKey(const Common::String &in) {
+	Common::String out;
+	bool pendingSpace = false, started = false;
+	for (uint i = 0; i < in.size(); ++i) {
+		const char c = in[i];
+		if (c == ' ' || c == '\r' || c == '\n' || c == '\t') {
+			pendingSpace = started; // ignore leading whitespace
+		} else {
+			if (pendingSpace)
+				out += ' ';
+			out += c;
+			pendingSpace = false;
+			started = true;
+		}
+	}
+	return out;
+}
+
+const char *SciEngine::getChtTranslation(const char *original) const {
+	// F8 toggled to English: report "no translation" so every caller falls back to the original.
+	if (!_chtLangOn)
+		return nullptr;
+	if (_chtTranslation.empty() || !original)
+		return nullptr;
+	Common::HashMap<Common::String, Common::String>::const_iterator it = _chtTranslation.find(sciChtNormKey(original));
+	if (it == _chtTranslation.end())
+		return nullptr;
+	return it->_value.c_str();
+}
+
+static Common::String unescapeCht(const Common::String &in) {
+	Common::String out;
+	for (uint i = 0; i < in.size(); ++i) {
+		// Big5 的第二個 byte 可以是 0x5C（「功」= A5 5C）。先整對抄過去，否則
+		// 「功」後面剛好接 n / t / \ 時那個 byte 會被當跳脫序列吃掉，整串錯位。
+		const byte lead = (byte)in[i];
+		if (lead >= 0xA1 && lead <= 0xF9 && i + 1 < in.size()) {
+			const byte trail = (byte)in[i + 1];
+			if ((trail >= 0x40 && trail <= 0x7E) || (trail >= 0xA1 && trail <= 0xFE)) {
+				out += in[i];
+				out += in[i + 1];
+				++i;
+				continue;
+			}
+		}
+		if (in[i] == '\\' && i + 1 < in.size()) {
+			char n = in[i + 1];
+			if (n == 'n') { out += '\n'; ++i; continue; }
+			if (n == 't') { out += '\t'; ++i; continue; }
+			if (n == '\\') { out += '\\'; ++i; continue; }
+		}
+		out += in[i];
+	}
+	return out;
+}
+
+void SciEngine::loadChtTranslation() {
+	// Runtime translation file: one entry per line, "<english>\t<big5 bytes>".
+	// TAB (0x09) and LF (0x0A) never occur inside Big5, so they are safe
+	// delimiters. Human-maintained source is UTF-8; the build tool converts it
+	// to this Big5 runtime form. Shipped in the game directory as CHT patch data.
+	Common::File f;
+	if (!f.open("translation.tsv"))
+		return;
+	int count = 0;
+	while (!f.eos()) {
+		Common::String line = f.readLine();
+		if (line.empty())
+			continue;
+		const char *s = line.c_str();
+		const char *tab = strchr(s, '\t');
+		if (!tab || tab == s)
+			continue;
+		Common::String key(s, tab - s);
+		Common::String val(tab + 1);
+		if (val.empty())
+			continue;
+		// Unescape "\\n"->newline and "\\\\"->backslash (SCI0/EGA strings contain
+		// hard line breaks, which cannot sit literally in a line-based TSV).
+		_chtTranslation[sciChtNormKey(unescapeCht(key))] = unescapeCht(val);
+		++count;
+	}
+	debug(1, "CHT: loaded %d translation entries", count);
+}
+
 Common::Language SciEngine::getLanguage() const {
+	// Traditional Chinese fan translation: allow forcing ZH_TWN via the target's
+	// "language" config key (e.g. --language=zh_TW) so the CHT patch can be applied
+	// on top of the original English game without a dedicated detection entry.
+	if (ConfMan.hasKey("language")) {
+		if (Common::parseLanguage(ConfMan.get("language")) == Common::ZH_TWN)
+			return Common::ZH_TWN;
+	}
 	return _gameDescription->language;
 }
 
diff --git a/engines/sci/sci.h b/engines/sci/sci.h
index d9361700..b35960ca 100644
--- a/engines/sci/sci.h
+++ b/engines/sci/sci.h
@@ -26,6 +26,8 @@
 #include "common/macresman.h"
 #include "common/util.h"
 #include "common/random.h"
+#include "common/hashmap.h"
+#include "common/hash-str.h"
 #include "sci/engine/vm_types.h"	// for Selector
 #include "sci/debug.h"	// for DebugState
 #include "sci/detection.h" // Shared code between detection and engine
@@ -185,6 +187,26 @@ public:
 	const char *getGameIdStr() const;
 	Common::Language getLanguage() const;
 
+	/**
+	 * Traditional Chinese fan-translation lookup. Returns the Big5-encoded
+	 * translation for an original (English) on-screen string, or nullptr if
+	 * there is none. Content-keyed so translations live in an external
+	 * translation.tsv rather than patched SCI resources.
+	 */
+	const char *getChtTranslation(const char *original) const;
+	void loadChtTranslation();
+
+	/**
+	 * F8 toggles between the Chinese translation and the original English, so players can
+	 * check the wording against the 1990 original. Deliberately a separate flag from
+	 * "is the translation loaded at all" — the language selection also gates Big5 drawing,
+	 * so reusing it here would change how text is rendered, not just which text is shown.
+	 * SCI keeps the text a script already handed to the graphics layer, so a toggle takes
+	 * effect on the next line rather than redrawing the current box.
+	 */
+	bool chtLangOn() const { return _chtLangOn; }
+	void toggleChtLang() { _chtLangOn = !_chtLangOn; }
+
 	/**
 	 * Returns true if the game's language direction is Right To Left.
 	 * RTL support did not exist in the original SCI engine.
@@ -406,6 +428,8 @@ private:
 
 	const ADGameDescription *_gameDescription;
 	const SciGameId _gameId;
+	Common::HashMap<Common::String, Common::String> _chtTranslation;  /**< CHT: English -> Big5 */
+	bool _chtLangOn = true;   /**< CHT: F8 toggle; false => show the original English */
 	ResourceManager *_resMan; /**< The resource manager */
 	ScriptPatcher *_scriptPatcher; /**< The script patcher */
 	EngineState *_gamestate;
